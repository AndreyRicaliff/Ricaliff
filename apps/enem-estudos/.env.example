# App pessoal de estudos ENEM — single-app, dados ficam no navegador (IndexedDB).
# Nenhuma variável é obrigatória hoje. Placeholders para evolução futura:

# Base path do app quando publicado em subdiretório (ex.: GitHub Pages)
# VITE_BASE_PATH=/enem-estudos/

# Chave de uma API de questões externa, se um dia integrar um banco público
# VITE_QUESTOES_API_URL=
